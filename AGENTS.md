# リポジトリ作業ルール

この `AGENTS.md` は、対応するAIコーディングエージェント間で共有するリポジトリ共通ルールである。
特定製品だけの設定は `.codex/` や `.github/` 側へ分離し、この文書には可能な限り共通方針だけを書く。

## リポジトリ知識

- 非自明な調査、デバッグ、設計、実装、外部仕様・GitHub Issue・技術記事の検証では、必要に応じて `repo-knowledge` Skill を使用する。
- `repo-knowledge` の正本は `.agents/skills/repo-knowledge/`。CodexとGitHub Copilotの両方から同じSkillを利用する。
- `docs/knowledge/` を無条件に一括読込しない。まずSkillの検索手順で候補を絞り、現在のタスクに必要な文書だけ読む。
- 過去知識は正解そのものではなく「再検証可能な手掛かり」として扱う。現在のコード、テスト、実行結果、現行の外部仕様と矛盾する場合は現在の根拠を優先し、知識を更新する。
- `repo-knowledge` が深い横断探索を必要と判断し、サブエージェント/カスタムエージェントが利用可能なら `knowledge-explorer` に探索を委譲する。利用できない場合はメインエージェントが対象を絞った検索を続行する。
- 実質的な作業の終了前に、将来のセッションで再利用価値のある知識が得られたかを判定する。価値がある場合のみ `docs/knowledge/` を作成または更新する。
- 一時的な進捗や現在タスクだけの作業メモは恒久知識に混ぜない。長期作業の進捗は `docs/plans/` 側に分離する。
- Copilot Memory、Codex Memories、チャット履歴など製品固有の記憶機能は補助として利用してよいが、共有・再現可能で必須の知識の正本はGit管理されるファイルとする。

## OS・実行環境

- WindowsとLinuxの両方で成立する手順を優先する。
- PowerShell専用、bash専用、`rg` などの追加コマンドを無条件に前提としない。
- 検索は、利用中のエージェントに組み込みの検索機能を優先し、必要なら `.agents/skills/repo-knowledge/scripts/search_knowledge.py` を使う。
- Skill付属Pythonスクリプトは Python 3.6+ / 標準ライブラリのみを前提とする。
- Pythonのコマンド名はOSごとに異なりうるため、利用可能なPython 3インタープリタを確認して実行する。代表例はLinuxの `python3`、Windowsの `py -3` または `python`。
- リポジトリ内の文書・metadataに記録するパスは、検索性とGit上の一貫性のため `/` 区切りを使う。

## 知識として残す基準

残す候補は、根本原因、非自明なリポジトリ挙動、設計判断と理由、再試行されそうな失敗、重要な同期関係、有効な検証・復旧手順、実装判断に影響する外部仕様である。

生ログ、長いコマンド出力、コードを読めば直ちに分かる内容、短期的な進捗、根拠のない推測は原則として保存しない。

## AutoHotkey変更時の必須検証

- `.ahk` ファイルを変更した場合は、対象スクリプトのAutoHotkeyメジャーバージョンを確認し、対応する実行ファイルで検証する。このリポジトリのv2スクリプトには、原則として `C:/Users/<username>/AppData/Local/Programs/AutoHotkey/v2/AutoHotkey64.exe` を使用する。
- 構文検証では、対象ファイルを直接常駐実行せず、一時的な `#Include` ラッパーを作成して `/ErrorStdOut` 付きで実行する。ラッパーには短時間で終了する `SetTimer(() => ExitApp(0), -100)` を含め、終了コード0を必須とする。
- 構文検証用の一時ファイルは、成功・失敗にかかわらず検証後に削除する。
- ライブラリファイルを変更した場合は、そのライブラリを読み込む影響範囲内のエントリースクリプトも構文検証する。
- Git管理下では、変更後に `git diff --check` を実行する。
- 構文検証に成功した後、変更対象が常駐スクリプトなら、そのスクリプトだけを `/restart` でリロードする。別の `.ahk` プロセスを終了・再起動しない。
- リロード後は少なくとも、対象スクリプトのプロセスが1個だけ存在すること、コマンドラインが対象ファイルを指すこと、プロセスが応答中であること、直後に終了していないことを確認する。
- 安全に自動化できる範囲で起動確認またはスモークテストを行う。外部操作や破壊的な副作用を伴うスクリプトは、明示的な許可なしに実動作させない。
- IME、UIA、ホットキー、マウス操作、対象アプリ上の表示など、実入力が必要な確認は自動検証と区別し、未実施の項目を「確認済み」と報告しない。
- 完了報告には、使用したAutoHotkey実行ファイル、構文検証の対象と終了コード、一時ファイルの削除、リロード対象、PID・プロセス数・応答状態、自動確認した範囲、ユーザー確認が必要な範囲を含める。
